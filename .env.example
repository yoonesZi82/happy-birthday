# Public site URL used inside invite QR codes (no trailing slash).
# On production this can be omitted — the current domain is used automatically.
# On localhost set this so QR codes never point to http://localhost:5173
VITE_PUBLIC_SITE_URL=https://your-domain.com
